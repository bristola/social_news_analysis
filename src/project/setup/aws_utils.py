

# pip install awscli boto3

class AWS_Utils:

    def __init__(self, key_name, machine_type, security_group, image_id, absolute_path_to_pem):
        self.key_name = key_name
        self.machine_type = machine_type
        self.security_group = security_group
        self.image_id = image_id
        self.absolute_path_to_pem = absolute_path_to_pem

    def create_instance():
        pass
